# Tomcat
server:
  port: 9200

# Spring
spring:
  application:
    # 应用名称
    name: shareus-auth
  profiles:
    # 环境配置
    active: dev
  cloud:
    nacos:
      discovery:
        # 服务注册地址
        server-addr: 124.220.67.51:8848
        namespace: 29ac7ced-c366-4f32-abbf-a767d50815af
      config:
        # 配置中心地址
        server-addr: 124.220.67.51:8848
        namespace: 29ac7ced-c366-4f32-abbf-a767d50815af
        # 配置文件格式
        file-extension: yml
        # 共享配置
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
